package com.example;

import java.util.List;
import java.util.stream.Collectors;

public class Main {
    public static void main(String[] args) {
        Expresion exp = new Expresion();

        // String secuencia1 = "{{}}{{}}";
        // List<Character> listaDeEntrada1 = secuencia1.chars().mapToObj(e->(char)e).collect(Collectors.toList());
        // System.out.println(exp.controlCorchetes(listaDeEntrada1));

        // String secuencia2 = "{{}{{}";
        // List<Character> listaDeEntrada2 = secuencia2.chars().mapToObj(e->(char)e).collect(Collectors.toList());
        // System.out.println(exp.controlCorchetes(listaDeEntrada2));
        
    }
}